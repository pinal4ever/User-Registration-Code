 CREATE DATABASE registration;


CREATE TABLE users (first_name VARCHAR(20), 
	            last_name  VARCHAR(20),
                    address_1 VARCHAR(20), 
	            address_2 VARCHAR(20),
                    city  varchar(20), 
		    state varchar(2),
                    zip   int,
                    country char(2)
                    );